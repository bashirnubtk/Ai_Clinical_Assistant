# এনভায়রনমেন্ট অ্যাক্টিভেট না থাকলে করে নিন
.\venv\Scripts\activate

ডাটাবেস চেক (যদি প্রয়োজন হয়)
python manage.py makemigrations
python manage.py migrate

# গিটহাব থেকে লেটেস্ট আপডেট নামানো
git pull origin main

# ১. সব পরিবর্তন যোগ করা
git add .

# ২. কি কাজ করেছেন তার একটি নোট দেওয়া
git commit -m "আজকের কাজের আপডেট"

# ৩. গিটহাবে পুশ করা
git push origin main

